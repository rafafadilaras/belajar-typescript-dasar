describe('Data Type', function () {
    it('should must declare', function () {
        const name = "Rafa Fadil Aras";
        const balance = 1000000;
        const boolean = true;
        console.info(name);
        console.info(balance);
        console.info(boolean);
    });
});
export {};
//# sourceMappingURL=tipe-data.test.js.map