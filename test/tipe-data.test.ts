describe ('Data Type', function() {
    it('should must declare', function() {
        const name: string = "Rafa Fadil Aras";
        const balance: number = 1000000;
        const boolean: boolean = true;
        
        console.info(name);
        console.info(balance);
        console.info(boolean);
    });
});